.venv/
__pycache__/
*.pyc

# Real bot data (including cached display names, in the same files) never gets committed.
/data/*.sqlite3

node_modules/

# Discord OAuth client secret / session signing key - see README "Authentication".
.env
